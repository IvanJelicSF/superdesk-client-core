; Several pinned dependencies (@sourcefabric/draft-js, react-autocomplete,
; react-sortable-hoc, ...) declare react ^16 peer ranges but run fine on
; React 17; webpack aliases react/react-dom to the root copy. Remove once
; those packages are upgraded or dropped during the Tiptap migration.
legacy-peer-deps=true
